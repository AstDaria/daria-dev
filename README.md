# Daria Dev — React + Vite + SCSS + TypeScript

- TS enabled (`.tsx`), strict mode, `vite.config.ts`
- Per-component folders with SCSS
- Ready for Vercel

## Dev

npm i
npm run dev

## Build

npm run build
npm run preview
